# Researcher guide

The implemented UI lists live TCGA projects and can request an immutable logical snapshot. Snapshot artifacts include the manifest, identity tables, file-to-sample links and modality coverage. The broader cohort, discovery, Finding Graph and reproduction interfaces described by the architecture are not yet complete; do not infer their availability from schemas alone.
